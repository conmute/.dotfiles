# default.conf — user pattern overrides (loaded after built-in defaults)
#
# Format: status:regex  (POSIX extended regex)
# Statuses: error, warning, input, success
#
# These extend the built-in patterns. Add your own below:

# Examples:
# warning:FIXME
# error:MyApp\.CriticalError
# success:deployment complete
# input:^mytool>
