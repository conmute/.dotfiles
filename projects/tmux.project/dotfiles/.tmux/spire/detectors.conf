# detectors.conf — enabled status detectors
# One detector per line. Comment out or remove to disable.
# Toggle live from the spire control panel.
#
# Available detectors:
#   asking      — AskUserQuestion (Claude needs your answer)
#   processing  — Claude is working (tools, thinking)
#   idle        — Claude waits for user input

asking
processing
idle
