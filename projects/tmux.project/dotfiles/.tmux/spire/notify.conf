# notify.conf — which events trigger notifications
#
# Format: <type>:<status>
# Types:  unread (badge), autofocus (switch to spire)
# Statuses: error, warning, input, success, idle

# Notify when pane goes idle (no output for timeout period)
unread:idle
